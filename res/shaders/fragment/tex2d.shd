#version 330 core

in vec2 interpolated_uv;

layout(location = 0) out vec4 color;

uniform sampler2D u_texture;

void main() {
    vec4 texel = texture2D(u_texture, interpolated_uv);
    color = texel;
}
