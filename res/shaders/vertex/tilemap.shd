#version 330 core

layout(location = 0) in vec4 position;

out vec2 interpolated_uv;

uniform mat4 u_proj, u_view;

void main() {
    gl_Position = u_proj * u_view * vec4(position.x, 0, position.y, 1.0);
    interpolated_uv = position.xy;
}
